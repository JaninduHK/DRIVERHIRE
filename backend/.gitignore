# Environment files — never commit real secrets (keep only examples)
.env
.env.*
!.env.example
!.env.*.example

# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*

# Local uploads / runtime data
uploads/
